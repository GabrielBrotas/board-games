/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",  // <=== enables static exports
  // reactStrictMode: true,
  // basePath: "/who-is-the-imposter",
};

export default nextConfig;
